<!DOCTYPE html>

<title>Promos</title>

{% block body %}
    <h1>Promos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ promo.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promos_index') }}">back to list</a>

    <a href="{{ path('app_promos_edit', {'id': promo.id}) }}">edit</a>

    {{ include('promos/_delete_form.html.twig') }}
{% endblock %}
